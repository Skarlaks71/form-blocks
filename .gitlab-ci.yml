image: node:latest

stages:
  - build

# Faz o build de todos os pacotes do monorepo
build_all:
  stage: build
  script:
    - npm install
    - npm run build
  artifacts:
    paths:
      - packages/*/dist/
  rules:
    - if: $CI_COMMIT_BRANCH
    - if: $CI_COMMIT_TAG